import type { Testimonial } from "@/types";

/**
 * TESTIMONIALS
 * -----------------------------------------------------------------------
 * Only add real testimonials from real people. The section hides itself
 * entirely while this array is empty — never fill it with placeholder
 * quotes for production.
 */
export const testimonials: Testimonial[] = [];
