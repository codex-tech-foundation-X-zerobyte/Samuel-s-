import type { Certification } from "@/types";

/**
 * CERTIFICATIONS & ACHIEVEMENTS
 * -----------------------------------------------------------------------
 * Optional — the Certifications section hides itself entirely if this
 * array is empty, per the brief ("do not display this section if there
 * is no data"). Add real entries here as you earn them:
 *
 * {
 *   title: "Certification Name",
 *   organization: "Issuing Organization",
 *   date: "Month Year",
 *   credentialUrl: "https://...", // optional
 *   description: "One line on what it covered.", // optional
 * }
 */
export const certifications: Certification[] = [];
